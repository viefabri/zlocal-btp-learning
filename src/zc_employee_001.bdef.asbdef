projection;
//strict ( 2 ); // 権限設定を省略するため、意図的にStrict Modeを下げています (本来は2推奨)
use draft;

define behavior for ZC_EMPLOYEE_001 alias Employee
{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}